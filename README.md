# flitter
A Twitter clone

# To run the server, cd to the server directory and run:
`npm run db:start`

`npm start`

# To stop the server
`Ctrl+C`

`npm run db:stop`

# When restarting the server
`npm run db:rm`

`npm run db:start`

`npm start`